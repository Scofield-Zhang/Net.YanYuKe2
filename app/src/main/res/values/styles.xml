<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@android:color/transparent</item>
        <item name="colorPrimaryDark">@color/main</item>
        <item name="colorAccent">@color/text_color_gray</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
    <style name="AppTheme1" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@android:color/transparent</item>
        <item name="colorPrimaryDark">@color/main</item>
        <item name="colorAccent">@color/text_color_gray</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>
    <!--    -->

    <!--在Android 4.4之前的版本上运行，直接跟随系统主题-->
    <style name="TranslucentTheme" parent="AppTheme"></style>

    <style name="CustomizedWindowTitleBackground">
        <item name="android:background">#047BF0</item>
    </style>

    <style name="titlebarstyle" parent="AppTheme">
        <item name="android:windowTitleSize">68dp</item>
        <item name="android:clipToPadding">true</item>
        <item name="android:windowTitleBackgroundStyle">@style/CustomizedWindowTitleBackground
        </item>
    </style>


    <!-- 弹窗的自定义样式 -->
    <style name="theme_dialog" parent="@android:style/Theme.Dialog">

        <item name="android:windowFrame">@null</item>

        <!-- <item name="android:windowIsFloating">true</item>-->

        <item name="android:windowIsTranslucent">false</item>

        <item name="android:background">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>


    </style>

    <style name="main_tab_center">
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/transparent</item>
        <item name="android:paddingTop">@dimen/bottom_tab_center_padding_up</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">fill_parent</item>
        <item name="android:button">@null</item>
        <item name="android:drawablePadding">@dimen/bottom_tab_padding_center_drawable</item>
        <item name="android:layout_weight">1.0</item>
    </style>


    <style name="wrap_xy">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
    <!--  包裹控件 -->
    <style name="fit_xy">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>
    <!-- 填充 -->
    <style name="fit_x">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
    <!-- 填充X -->
    <style name="main_tab_bottom">
        <item name="android:textSize">@dimen/bottom_tab_font_size</item>
        <item name="android:ellipsize">marquee</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/transparent</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item><!--  修改 -->
        <item name="android:singleLine">true</item>
        <item name="android:textColor">@color/selector_tab_text</item>

        <item name="android:drawablePadding">@dimen/bottom_tab_padding_drawable</item>

    </style>
    <!--
<item name="android:textColor">@color/main</item>
    <item name="android:layout_weight">1.0</item>
    <item name="android:paddingTop">@dimen/bottom_tab_padding_up</item>
    -->
    <!---->
    <!--    去按钮立体效果    <item name="android:button">@null</item>-->
    <style name="main_menu_animstyle">
        <item name="android:windowEnterAnimation">@anim/windows_show</item>
        <item name="android:windowExitAnimation">@anim/windows_hide</item>
    </style>

    <style name="AnimBottom" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/windows_show</item>
        <item name="android:windowExitAnimation">@anim/windows_hide</item>
    </style>

    <style name="transparentFrameWindowStyle" parent="android:style/Theme.Dialog">
        <item name="android:windowBackground">@drawable/photo_choose_bg</item>
    </style>

    <!-- 加载进度对话框 -->
    <style name="MyLoadDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:background">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:gravity">bottom</item>
    </style>
</resources>
